/**
 * DescriptionList — labelled details about one record.
 *
 * § CONTRACT
 * The root is a dl. Each DescriptionItem contains a dt and dd. Values accept
 * arbitrary content, including links, badges, controls, and explicit absence.
 * Layout is stacked on narrow screens. Nothing invents a value for empty data.
 *
 * § MECHANICS
 * Native semantics with CSS grid; no object introspection or field-name labels.
 */
export {};
