/**
 * DateRangePicker follows the date-only contract in ../date-picker/doc.ts.
 * A range is one compound value, so choosing a start alone does not publish
 * an incomplete object. The field engine owns the temporary calendar anchor;
 * the caller owns a complete inclusive pair or null.
 */
export {};
