/**
 * MultiSelect uses the searchable-choice contract in ../combobox/doc.ts.
 * Its selected values are rendered separately from the search input: filtering
 * must not hide the set a user is about to submit. The shared engine keeps the
 * single and multiple controls' focus and form behavior from drifting apart.
 */
export {};
