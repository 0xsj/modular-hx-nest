/**
 * Typography — semantic elements with an independent visual scale.
 *
 * § CONTRACT
 * Heading requires a level (1–6); size never changes that level. Text defaults
 * to a paragraph and may render inline. SectionLabel is a paragraph, never an
 * implied heading. Native attributes and caller classes remain available.
 * Tone, size, and weight use the existing tokens. Truncation is opt-in.
 *
 * § MECHANICS
 * Variant maps select CSS Module classes. These components have no client
 * state, read no route, and can be rendered directly by a server component.
 */
export {};
