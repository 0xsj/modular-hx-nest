/** Public entry contract: / introduces Flover as a Next.js template and stays
 * at the root address. A short description leads to the public kitchen sink,
 * cookbook and manual. No account or backend read is needed to explore them.
 * Reuse the existing type, button and layout primitives with responsive,
 * theme-aware tokens. Restore the original accent-gradient background with a
 * larger flover-next heading to identify this framework variant.
 * The workspace rail belongs to the destinations, not this
 * introduction. Declared before replacing the cookbook redirect.
 */
export {};
